# Test CMakeLists.txt for ESP-IDF Unit Tests
# This file is used to build the test version of the firmware

cmake_minimum_required(VERSION 3.16)

# Set the main component for tests - exclude the original main
set(EXTRA_COMPONENT_DIRS test_main)
set(EXCLUDE_COMPONENTS main)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(firmware_tests)